/** Small re-exports so client code imports from one place (and tests can mock it). */
export { createHttpAdapter } from '@mailmotion/storage';
export { toHttpsUrl as toHttpsUrlOrNull } from '@mailmotion/schema';
